/// <reference path="utility-functions.ts" />

const result = Utility.maxBooksAllowed(33);
console.log(result);

import util = Utility.Fees;

const result2 = util.calculateLateFee(20);
console.log(result2);